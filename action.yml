name: 'Configure ssh'
description: ''
inputs:
  host:
    description: 'Host'
    required: true
  username:
    description: 'User'
    default: 'ubuntu'
  key:
    required: true
    default: 'Key'
runs:
  using: "composite"
  steps:
    - run: ${{ github.action_path }}/configure.sh
      shell: bash
      env:
        SSH_USER: ${{ inputs.USERNAME }}
        SSH_KEY: ${{ inputs.KEY }}
        SSH_HOST: ${{ inputs.HOST }}